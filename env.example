# iv-suggest settings. Install as /etc/iv-suggest/env, mode 600.
# Every value is documented once, in docs/CONFIG.md.

# REQUIRED. The account the bot belongs to (the users.email value).
IV_SUGGEST_ACCOUNT=

# Optional, shown with their defaults.
#IV_SUGGEST_API=http://localhost:3000
#IV_SUGGEST_COMPOSE_DIR=/root/docker/youtube
#IV_SUGGEST_DB_SERVICE=invidious-db
#IV_SUGGEST_DB_USER=kemal
#IV_SUGGEST_DB_NAME=invidious
#IV_SUGGEST_CONFIG=/etc/iv-suggest/lanes.yml

# Optional. Only a fallback for an install that has not run `init` since the
# bot started minting its own session per account. Raw JSON, not base64.
#IV_SUGGEST_TOKEN=
